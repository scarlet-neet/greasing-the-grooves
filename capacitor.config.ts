import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scarletneet.app',
  appName: 'GTG Scarlet NEET',
  webDir: 'dist/client'
};

export default config;
